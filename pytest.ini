[pytest]
asyncio_mode = auto
# Silences pytest-asyncio's "asyncio_default_fixture_loop_scope is unset"
# deprecation warning. "function" matches what auto mode already does by
# default, so this is a no-op behavior-wise - just makes it explicit.
asyncio_default_fixture_loop_scope = function
testpaths = tests
